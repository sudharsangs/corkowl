const config = {
  primaryColor: "#811434",
  secondaryColor: "#EF4859",
  sidebarSelectedColor: "#F7F1F3",
  bodyTextColor: "#485572",
  subtleTextColor: "#485572",
  darkColor: "#262730",
  blackColor: "#000000",
  successColor: "#2FCA72",
  whiteColor: "#FFFFFF",
  lightStateColor: "#FAFCFE"
};

export const tokens = {
  backgroundColor: {
    primary: config.primaryColor,
    secondary: config.secondaryColor,
    sidebarSelected: config.sidebarSelectedColor,
  },
  color: {
    primary: config.primaryColor,
    secondary: config.secondaryColor,
    bodyText: config.bodyTextColor,
    subtleText: config.subtleTextColor,
    darkText: config.darkColor,
    blackText: config.blackColor,
    successText: config.successColor,
    whiteText: config.whiteColor,
    lightState: config.lightStateColor
  },
};
