const config = {
  primaryColor: "#811434",
  secondaryColor: "#EF4859"
};

export const tokens = {
  backgroundColor: {
    primary: config.primaryColor,
    secondaryColor: config.secondaryColor
  },
};
